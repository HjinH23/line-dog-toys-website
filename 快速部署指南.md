# 快速部署到Vercel - 完整步骤

## 前提准备

### 1. 安装Git

Git是版本控制工具,部署前必须安装:

1. 访问 [https://git-scm.com/download/win](https://git-scm.com/download/win)
2. 下载并安装Git for Windows
3. 安装时保持默认选项即可
4. 安装完成后重启PowerShell

验证安装:
```bash
git --version
```

### 2. 注册GitHub账号

1. 访问 [https://github.com](https://github.com)
2. 点击 "Sign up" 注册
3. 验证邮箱

### 3. 注册Vercel账号

1. 访问 [https://vercel.com](https://vercel.com)
2. 点击 "Sign Up"
3. **推荐**: 选择 "Continue with GitHub" (用GitHub账号登录)

---

## 部署步骤(5分钟完成)

### 步骤1: 初始化Git仓库

在项目目录打开PowerShell,执行:

```bash
cd e:\Antigravity\code\project1

# 初始化Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 线条小狗毛绒玩具官网"
```

### 步骤2: 创建GitHub仓库

1. 登录GitHub
2. 点击右上角 "+" → "New repository"
3. 填写信息:
   - Repository name: `line-dog-toys-website`
   - Description: `线条小狗毛绒玩具公司官网`
   - Public (公开)
4. **不要**勾选 "Add a README file"
5. 点击 "Create repository"

### 步骤3: 推送代码到GitHub

复制GitHub页面显示的命令,或执行:

```bash
# 添加远程仓库(替换YOUR_USERNAME为你的GitHub用户名)
git remote add origin https://github.com/YOUR_USERNAME/line-dog-toys-website.git

# 推送代码
git branch -M main
git push -u origin main
```

如果提示输入用户名和密码:
- Username: 你的GitHub用户名
- Password: 使用Personal Access Token(不是密码)
  - 获取Token: GitHub → Settings → Developer settings → Personal access tokens → Generate new token

### 步骤4: 在Vercel部署

1. **登录Vercel**
   - 访问 [https://vercel.com](https://vercel.com)
   - 用GitHub账号登录

2. **导入项目**
   - 点击 "Add New..." → "Project"
   - 在列表中找到 `line-dog-toys-website`
   - 点击 "Import"

3. **配置项目**
   - Project Name: `line-dog-toys` (可自定义)
   - Framework Preset: Next.js (自动检测)
   - Root Directory: `./`
   - Build and Output Settings: 保持默认
   - Environment Variables: 暂时不需要

4. **开始部署**
   - 点击 "Deploy" 按钮
   - 等待2-3分钟

5. **完成!**
   - 部署成功后会显示:
     ```
     🎉 Congratulations!
     Your project has been deployed
     ```
   - 获得网址,例如: `https://line-dog-toys.vercel.app`

---

## 测试网站

部署完成后:

1. 点击Vercel提供的网址
2. 测试所有页面:
   - ✅ 首页
   - ✅ 产品展示
   - ✅ 关于我们
   - ✅ 联系方式
3. 在手机上打开网址测试
4. 分享给朋友测试

---

## 后续更新网站

每次修改代码后:

```bash
# 1. 保存所有文件

# 2. 提交更改
git add .
git commit -m "描述你的更改"

# 3. 推送到GitHub
git push
```

Vercel会自动检测到更新并重新部署,1-2分钟后网站就会更新!

---

## 自定义域名(可选)

如果你有自己的域名(如 `www.linedogtoys.com`):

1. 在Vercel项目页面点击 "Settings" → "Domains"
2. 输入你的域名
3. 按照提示在域名服务商处添加DNS记录:
   - Type: `CNAME`
   - Name: `www` (或 `@`)
   - Value: `cname.vercel-dns.com`
4. 等待DNS生效(5-30分钟)

---

## 常见问题

### Q: Git推送时要求输入密码?

A: GitHub已不支持密码登录,需要使用Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. 勾选 `repo` 权限
4. 复制生成的token
5. 在密码处粘贴token

### Q: Vercel部署失败?

A: 检查:
1. `package.json` 中的依赖是否正确
2. 是否有语法错误
3. 查看Vercel的错误日志

### Q: 网站访问很慢?

A: Vercel使用全球CDN,首次访问可能稍慢,后续会很快。

### Q: 如何查看访问统计?

A: Vercel项目页面 → Analytics 标签

---

## 下一步优化

部署成功后可以:

1. ✅ 添加Google Analytics统计
2. ✅ 提交到Google Search Console(SEO)
3. ✅ 添加网站图标(favicon)
4. ✅ 配置自定义域名
5. ✅ 添加更多产品图片

---

## 需要帮助?

如果遇到问题:
1. 查看Vercel的部署日志
2. 检查GitHub仓库是否正确推送
3. 告诉我具体的错误信息

**恭喜!** 完成这些步骤后,您的网站就可以被全世界访问了! 🎉
